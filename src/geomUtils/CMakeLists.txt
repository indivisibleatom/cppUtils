file(GLOB GEOMUTILS_SRC
  "src/*.cpp")

#add_library (geomUtils STATIC ${GEOMUTILS_SRC})

#target_include_directories (geomUtils PUBLIC "${PROJECT_SOURCE_DIR}/inc/geomUtils"
#                                             "${PROJECT_SOURCE_DIR}/inc/utils")
